#cloud-config
autoinstall:
  version: 1
  locale: it_IT.UTF-8
  # Tentative fix for missing '127.0.0.1 localhost' from all VMs deployed with this template
  manage_etc_hosts: localhost
  keyboard:
    layout: it
  ssh:
    install-server: true
    allow-pw: false
    disable_root: true
    ssh_quiet_keygen: true
    allow_public_ssh_keys: true
  packages:
    - qemu-guest-agent
  storage:
    layout:
      name: direct
    swap:
      size: 0
  user-data:
    package_upgrade: false
    timezone: Europe/Rome
    users:
      - name: ansible
        groups: [sudo]
        lock-passwd: false
        shell: /bin/bash
        passwd: "<REDACTED>"
        ssh_authorized_keys:
          - "<REDACTED>"
